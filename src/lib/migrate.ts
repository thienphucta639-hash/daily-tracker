// Migration disabled — do NOT auto-modify user data
// formatDate() already fixed to use local timezone
// Old data stays as-is, new data will be correct

export function migrateTimezone() {
  // No-op: never touch user's localStorage data automatically
}
